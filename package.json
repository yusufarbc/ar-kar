{
  "name": "ar-kar",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test": "npm run check && npm run build",
    "deploy:production": "npm run build && wrangler pages deploy dist --project-name=ar-kar --branch=production --skip-caching",
    "deploy:staging": "npm run build && wrangler pages deploy dist --project-name=ar-kar --branch=staging --skip-caching",
    "panel:deploy": "wrangler pages deploy panel --project-name=ar-kar-admin --branch=production"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.10",
    "astro": "^7.3.3",
    "typescript": "^5.7.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260918.1",
    "@types/node": "^22.13.10",
    "wrangler": "^4.134.0"
  }
}
